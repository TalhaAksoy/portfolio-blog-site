---
title: "TanStack Table Nedir, Nasıl Kullanılır?"
date: "2025-08-25"
tags: ["tanstack", "react", "frontend", "table", "tutorial"]
summary: "TanStack Table, tablo işlevlerini (sıralama, filtreleme, sayfalama) yöneten güçlü bir kütüphane. Bu yazıda nasıl kullanacağınızı adım adım gösteriyorum."
---

Modern web uygulamalarında verilerin tablo halinde gösterimi neredeyse her projede karşımıza çıkar. Ancak tablolar sadece verileri listelemekten ibaret değildir; **filtreleme, sıralama, arama, sayfalama** gibi etkileşimli özellikler kullanıcı deneyimini doğrudan etkiler.

Bu noktada **TanStack Table** devreye giriyor. Kütüphane, tüm bu temel işlevleri sizin yerinize hallederken, size sadece **UI tarafını özgürce tasarlamak** kalır.

---

## Örnek Proje

📌 Aşağıda anlatılan kodu canlı olarak incelemek için: [*TanStack Table Demo*](https://codesandbox.io/p/sandbox/hdswpw?file=%2Fsrc%2FApp.tsx%3A79%2C16)

---

## TanStack Table Nedir?

TanStack Table, tablonun temel mekaniklerini yöneten **headless (görünümsüz)** bir kütüphanedir.

- Tablonun veriye dayalı tüm işlevlerini (filtreleme, sıralama, arama, sayfalama) kendisi üstlenir.
- Görsel taraf tamamen geliştiriciye bırakılmıştır.

Yani TanStack ile güçlü bir tablo altyapısı kurabilir, görünümü istediğiniz kütüphane veya stil çözümüyle oluşturabilirsiniz.

Örneğin:
- Hazır table bileşenleri: **ShadcnUI Table**, **MUI Table**
- Kendi tasarımınız: **TailwindCSS** veya saf CSS

---

## TanStack Table Nasıl Kullanılır?

### 1. Kurulum

```bash
npm i @tanstack/react-table
# veya
yarn add @tanstack/react-table
```

---

### 2. Import İşlemleri

```tsx
import * as React from "react";
import {
  ColumnDef,
  flexRender,
  getCoreRowModel,
  getSortedRowModel,
  getFilteredRowModel,
  getPaginationRowModel,
  useReactTable,
  type SortingState,
  type ColumnFiltersState,
} from "@tanstack/react-table";
import "./styles.css";
```

---

### 3. Veri Tipi ve Örnek Data

```tsx
type User = {
  id: string;
  fullName: string;
  email: string;
  role: "admin" | "editor" | "viewer";
};

const data: User[] = [
  {
    id: "1",
    fullName: "Ada Lovelace",
    email: "ada@calc.io",
    role: "admin",
  },
  {
    id: "2",
    fullName: "Grace Hopper",
    email: "grace@navy.mil",
    role: "editor",
  },
  // ...
];
```

---

### 4. Kolon Tanımları

```tsx
const columns = React.useMemo<ColumnDef<User>[]>(
  () => [
    { accessorKey: "fullName", header: "Name" },
    { accessorKey: "email", header: "Email" },
    { accessorKey: "role", header: "Role" },
    {
      id: "accountAgeDays",
      header: "Age (days)",
      accessorFn: (row) =>
        Math.floor(Math.random() * (100 - 10 + 1) + 10),
      cell: (ctx) => ctx.getValue<number>(),
    },
  ],
  []
);
```

---

### 5. State ve Table Konfigürasyonu

```tsx
const [sorting, setSorting] = React.useState<SortingState>([]);
const [columnFilters, setColumnFilters] = React.useState<ColumnFiltersState>([]);
const [globalFilter, setGlobalFilter] = React.useState("");
const [pagination, setPagination] = React.useState({
  pageIndex: 0,
  pageSize: 5,
});

const table = useReactTable({
  data,
  columns,
  state: { sorting, columnFilters, globalFilter, pagination },
  onSortingChange: setSorting,
  onColumnFiltersChange: setColumnFilters,
  onPaginationChange: setPagination,
  onGlobalFilterChange: setGlobalFilter,
  getCoreRowModel: getCoreRowModel(),
  getSortedRowModel: getSortedRowModel(),
  getFilteredRowModel: getFilteredRowModel(),
  getPaginationRowModel: getPaginationRowModel(),
  autoResetPageIndex: false,
});
```

---

### 6. Render İşlemleri

#### Global Search

```tsx
<input
  className="control-input"
  value={globalFilter ?? ""}
  onChange={(e) => table.setGlobalFilter(e.target.value)}
  placeholder="Search all columns..."
/>
```

---

#### Role Filtreleme

```tsx
<select
  className="control-select"
  value={(table.getColumn("role")?.getFilterValue() as string) ?? ""}
  onChange={(e) =>
    table.getColumn("role")?.setFilterValue(e.target.value || undefined)
  }
>
  <option value="">All roles</option>
  <option value="admin">admin</option>
  <option value="editor">editor</option>
  <option value="viewer">viewer</option>
</select>
```

---

#### Header

```tsx
<thead>
  {table.getHeaderGroups().map((hg) => (
    <tr key={hg.id}>
      {hg.headers.map((h) => (
        <th
          key={h.id}
          onClick={h.column.getToggleSortingHandler()}
          className={h.column.getCanSort() ? "sortable" : ""}
          aria-sort={
            h.column.getIsSorted() === "asc"
              ? "ascending"
              : h.column.getIsSorted() === "desc"
              ? "descending"
              : "none"
          }
        >
          {flexRender(h.column.columnDef.header, h.getContext())}
          {{
            asc: " ↑",
            desc: " ↓",
          }[h.column.getIsSorted() as "asc" | "desc"] ?? null}
        </th>
      ))}
    </tr>
  ))}
</thead>
```

---

#### Body

```tsx
<tbody>
  {table.getRowModel().rows.map((row) => (
    <tr key={row.id}>
      {row.getVisibleCells().map((cell) => (
        <td key={cell.id}>
          {flexRender(cell.column.columnDef.cell, cell.getContext())}
        </td>
      ))}
    </tr>
  ))}

  {table.getRowModel().rows.length === 0 && (
    <tr>
      <td className="empty" colSpan={columns.length}>
        No data
      </td>
    </tr>
  )}
</tbody>
```

---

#### Pagination

```tsx
<div className="pagination">
  <button
    className="btn"
    onClick={() => table.previousPage()}
    disabled={!table.getCanPreviousPage()}
  >
    Prev
  </button>
  <span className="page-info">
    Page {table.getState().pagination.pageIndex + 1} / {table.getPageCount()}
  </span>
  <button
    className="btn"
    onClick={() => table.nextPage()}
    disabled={!table.getCanNextPage()}
  >
    Next
  </button>
</div>
```

---

## Sonuç

TanStack Table, **güçlü ama esnek** bir yapıya sahip. Siz sadece verilerinizi ve kolonlarınızı tanımlıyorsunuz; sıralama, filtreleme, sayfalama gibi temel işlevleri kütüphane sizin için yönetiyor.

Böylece **UI tarafında tamamen özgürsünüz**: ister hazır kütüphaneleri kullanabilir, ister sıfırdan kendi tasarımınızı oluşturabilirsiniz.

Ben kendi deneyimlerime dayanarak en temel kısımları aktarmaya çalıştım. Daha fazla sorunuz olursa yorumlarda paylaşabilirsiniz — elimden geldiğince cevaplarım.
